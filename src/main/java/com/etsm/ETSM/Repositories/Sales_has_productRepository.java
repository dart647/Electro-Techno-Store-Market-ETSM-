package com.etsm.ETSM.Repositories;

import com.etsm.ETSM.Models.Sales_has_product;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Sales_has_productRepository extends JpaRepository<Sales_has_product, Long> {
}
